# Example environment variables (DO NOT put real secrets in git)

# Required (LLM script generation)
OPENAI_API_KEY="sk-your-openai-api-key"

# Optional (Cloud Run recommended: do NOT set GOOGLE_APPLICATION_CREDENTIALS)
# For local development you can use ADC instead:
#   gcloud auth application-default login
# Or set a service account json path:
# GOOGLE_APPLICATION_CREDENTIALS="/path/to/key.json"
GOOGLE_CLOUD_PROJECT="your-gcp-project-id"

# Optional (uploads + signed urls)
GCS_BUCKET_NAME="your-gcs-bucket-name"

